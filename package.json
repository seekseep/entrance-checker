{
  "name": "entrancer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run copy-models && parcel public/*.html -p 3000",
    "build": "npm run copy-models && parcel build public/*.html",
    "copy-models": "node ./scripts/copy-models.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "@vladmandic/face-api": "^1.6.10",
    "jsqr": "^1.4.0"
  }
}
